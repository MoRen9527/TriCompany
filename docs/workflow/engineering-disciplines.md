# TriCompany 工程纪律集（三端通用）

> sourceOfTruth: TriCompany/docs/workflow/engineering-disciplines.md
> syncMode: source-only
> lastSyncedAt: 2026-09-23（D-33 管道截胡退出码禁令入册——git 断言禁走管道尾/展示断言分离/高危推拉近端复核；PUSH-RC 假红案）
> 性质：跨域工程纪律（编排层/TriLC/TriMC 员工通用）——从编排层会话记忆升级为公司资产。员工知识工作区同步路径：合同/培训文档引用本文件。

## 为什么有这个文件

纪律最初在编排层（本地研发仓 claude code）会话记忆中沉淀，但 TriLC/TriMC 侧员工不可见——**跨域通用纪律必须落在 TriCompany 真源**，三端员工经各自知识通道读取。

## 纪律条目

### D-01 subagent 落盘纪律（2026-08-17，三次事故提炼）

长任务 agent context 耗尽时，**口头"已落盘"不可信**（总结文本先于写盘生成，断点常落在两者之间）。

- agent 侧：**先写文件、后写总结**——总结必须带文件路径 + 行数/大小证据
- 收稿侧：**三查**（文件存在 / 内容规模 / 与报告口径一致）后才采信
- **v2 增补（2026-09-05，BOD 点单④⑥）**：完成判定以**机械回执**为记账依据——派单/协作的完成证据=SendMessage 回执/msg 锚等机器凭证；口头/转述「已办」不作完成证据（「回报声称≠实际执行」——本条「先写后报」同族主语的回报面；台账 LG-024/031/032 修订史锚）
- 长任务（50+ 项/多文件）分段落盘（每 ~20 条），断点可续
- 兜底：发现未落盘直接接续生成（机械转化类最快），不返工原 agent

### D-02 cron job state 卫生（2026-08-17，weekly 三夜未触发根因）

手动改 cron job state 时**禁抹 `nextRunAtMs`**——引擎靠它排期，缺失=永不调度。只 patch 目标字段；改 cron 表达式后重算 nextRunAtMs；诊断口诀：cron 型 job 不触发先查该字段。

### D-03 daemon 重启纪律（2026-08-16）

重启 TriLC daemon 走 `trilc stop` → `.cmd` 拉起两步（pidfile 权威路径），禁裸杀进程（pidfile 与监听进程错位→新旧进程身份混乱→"补丁没生效"假象）。

**v2 增补（2026-08-28，LG-002 首跑 T3 失败复盘）**：`setx` 后**经 shell 直启的进程继承本 shell 的 env 快照**（读不到 setx 新值）——daemon 与扩展宿主同律。重启前必须在会话内显式从注册表读入新 env（或以读注册表的方式拉起），否则 token/配置类变更出现"已 setx 却 401"的假故障。

**v3 增补（2026-08-28，LG-012 restart 崩循环复盘，FADE-006 重建体提案采纳）**：dist 形态服务（gitignored 构建产物）的 restart 前置检查必须含两项——①dist 完整性 ②node_modules 符号链接目标存在性。**对 gitignore 构建产物仓做 reset/re-checkout 类操作后必须重建 dist**（旧进程内存存活会长期掩盖潜伏损坏，"8-26 起首次 restart 必炸"即此形态）。实施：TriMC restart 前置检查入 runbook；TriModel 类仓重检出 ops 附加 npm run build 步骤。

### D-04 时刻引用纪律（2026-08-17，编排层报时错 8 小时根因）

上下文混存本地时刻与 UTC ISO 时间戳（teammate 消息/日志 `Z` 后缀）——**报时刻前必现查系统时钟**，禁止从上下文时间戳外推；引用 ISO 时间戳必须认 `Z` 并按本地时区换算（中国 +8）。

**v2 增补（2026-08-24，CEO 质询"时区时间不准"复盘）**：报时三要素——**现查、标注、不外推**。① 凡报时标注读数时刻与来源（如"09:25 北京（09:25:15Z 实测）"）；② 禁止用早前读数外推"现在几点"（外推 + 会话隔夜续读 = 错小时级表象；根因是陈旧引用，非钟不准——当日实测三方钟 NTP 级精准）；③ 机器链路（cron/commit/.shift-ade 时间戳）不依赖人肉报时，报时仅是沟通注释。防线配套：TriMC 服务器 cron job clock-skew-check 每小时自动钟差告警（>60s 邮件 + healthz degraded 双通道）。

**v3 增补（2026-08-27，FADE 值班误报"僵了 8 小时"复盘）**：**换算一次、单一时区帧内比较**。日志/时间戳一律先换成北京时间再进入叙述；禁止把 UTC 数值与北京时钟读数直接相减得"滞后时长"（01:18Z 对 09:24 读出 8 小时即此病灶——实际只差 6 分钟）。跨时区推理时强制自问：两个数是否同一时区帧？

**v4 增补（2026-08-28，CEO 提案：双轨时刻制）**：时刻呈现按**受众分轨**——
- **机器轨**：JSON 合同（envelope check_time / 评分卷宗 / registry / 审计文件）与需字典序排序的字段，一律 ISO8601 UTC Z（`2026-08-28T15:12:47Z`），不改；
- **人读轨**：面向 CEO/员工呈现的 markdown 报告、树 resultNote、战役记录、log.md 表格、通知正文——一律北京时间对齐（`2026-08-28 23:12 +08`），可括注 UTC 原值（`(15:12Z)`）供机器复核；
- **衔接件**：§2.7 节点收口报告的 ```json 机读核心走机器轨，散文叙述走人读轨——同一报告内两轨并存合法，但同一句内禁止混用。

**v6 增补（2026-09-05，BOD 点单④⑤；BOD 算验细则②）**：星期/日期派生标签**两步算验**——先现查「今天几号」，再算「星期几」，禁从上下文惯性继承标签（同窗 COS 席星期标签惯性复发实证；本条「禁外推」原则的日粒度变体）。

**v7 增补（2026-09-14，CTO 起草+BOD 转办+CAO 会签入族；源案=CTO 席 09-14 时点申报失实事件）——AI 时间感知特性补注（感知缺陷非诚信缺陷）**：

根因定性：LLM 无内源时钟——「时间感」系文本语境外推，与物理时钟无耦合；长任务流中体感耗时系统性膨胀（实证：真实 88min→申报 ~4.5h；长批次复利漂移最危）。**这是感知缺陷，非诚信缺陷**；对治在机制层（绑执行证据），不在道德层。

机制绑定五条：①**自证格式**：读时必绑执行证据——「现查=`<date 输出行原样>`（本回合执行）」；无本回合 date 执行一律写「未现查」（禁留空/推算/假出处）。②**禁占位符**：「HH:Mx」类含未定数位的时间禁进「现查」位。③**批量重锚**：长批次每 5 笔回执强制重跑一次 date。④**跨席抽验**：接收方保留抽验权（三源交叉类）；树销账/呈报/审计件等**高影响申报必抽**。⑤**禁二次加工**：datetime 原样粘贴，禁换算/取整/外推；双轨呈现的括注（v4）照旧，加工禁于原值位。

**判读序**（未来同类案例）：①先按本补注定性为感知缺陷；②查机制绑定是否被**无意**绕过→按纪律事件处理；③**仅当证明机制被故意绕过**（如伪称执行）才升级为诚信问题。判读三档均以**机械证据链**为依据（date 执行记录/回执/三源交叉），**禁以当事席自述定性**；感知缺陷定性**不豁免**机制执行义务（对口归因，不减义务）。

适用范围：D-04 族时点申报全覆盖（FADE 实例与席位协作场景）；判读序援引域=时点申报族，跨域同类另行裁定。源案证据在卷=CTO 席时点全量审计（33 条三档勘正）+W38 补注草案（d04-ai-time-perception-note-draft.md）。

### D-05 git index 共享卫生（2026-08-14，三次 index 污染提炼）

多 agent 共享仓：统一 `git add <明确路径>` + `git commit`，禁 `git commit -- <path>`；commit 前三查（status/cached diff/log）。

### D-07 live entry 派生壳纪律（2026-08-19，live entry 评审裁决）

live entry（`.github/agents/*.agent.md`）是员工 contract 的**派生加载壳**——信息真源收敛到 contract（三端可读），live entry 只承载当前宿主（Copilot-host）发现面。**禁人工直接编辑 live entry**：改动一律走源侧（source-agents 五件套/contract）后经 `source_publish_check --publish-agents` 发布；hash 不一致时下次 publish 覆盖 + 审计留痕（防双真源漂移）。三层语义：名册=决策真源、contract=信息真源、live entry=适配面。退役时点=TriMC 正式宿主切换（不设独立退役项）。

### D-06 共学周记记录纪律（2026-08-18，W34 首写违规立册）

「记入周记/共学」类动作**先查规范再动笔**：必读 prompt 固定格式（`TriMetaverse/.github/prompts/项目级 AI 共学周记.prompt.md`）+ 归档 README + **最近一个已存在周**的周记（格式随周演进，禁止跨多周翻旧模板）；条目用固定五件结构（现象/具体表现/解决方案/问题影响 + 当前经验{项目经验,模型自查}）；落当周目录、只追加不重写；内部工程台账（commit 索引）不入册。完整动作规范（Qualify 四问/Plan 三查/Close 五查/终态）：`TriMetaverse/docs/workflow/operating-records/项目级 AI 共学周记/ade-journal-recording-spec.md`；完整 ADE 正典链：登记（CLI begin 生成 runId）→ agent Qualify/Plan（语义四问 + entry.json）→ DCE（CLI qualify/append，格式由代码保证）→ **Agent Close Skill（读回语义裁决 approved|escalated）→ Close CLI（校验裁决 + run 链 + 五查 → 终态 APPROVED/ESCALATED）**——agent 收口在前，CLI 收口在后，CLI 是裁决的校验者不是发起者。执行体：`TriMetaverse/scripts/journal/journal-cli.mjs`。

### D-08 git hook 内跨仓操作必须 unset GIT_DIR 系变量（2026-08-27，FADE hook pull 全静默失败）

git 给 post-receive 等 hook 进程注入 `GIT_DIR`（指向裸仓），优先级高于 `git -C` 的目录探测——hook 里对**其他**工作仓执行 fetch/rebase 会全部打回裸仓且零报错。行为规则：hook 脚本 shebang 后立即 `unset GIT_DIR GIT_WORK_TREE GIT_INDEX_FILE GIT_OBJECT_DIRECTORY GIT_ALTERNATE_OBJECT_DIRECTORIES`；子进程继承干净环境一并得救。

### D-09 含中文的 .ps1 必须 UTF-8 带 BOM（2026-08-27，fade-watch 解析崩溃）

Windows PowerShell 5.1 对无 BOM 文件按 ANSI/GBK 解码，中文注释/字符串乱码可吞引号与花括号（"string is missing the terminator" 类解析错误）。pwsh 7 无此问题。行为规则：含非 ASCII 的 .ps1 写完立即补 BOM（`[IO.File]::WriteAllText($p, [IO.File]::ReadAllText($p,[Text.Encoding]::UTF8), [Text.UTF8Encoding]::new($true))`），提交前用 powershell.exe 最小调用冒烟一次。

### D-10 共享裸仓权限卫生——混合身份 push 后的自愈（2026-08-27，p0fix1 push 被拒复盘）

多身份（root 终端 + fleet 服务）共写同一批 bare 仓时，高权用户 push 会落下 root:root 新对象段，低权用户随后 push 撞 objects Permission denied。行为规则：① bare 仓统一 `core.sharedRepository=group` 且属主收敛 fleet 组；② 无法消除混合身份时，部署周期性自愈（sg-server crontab `bare-perm-heal`：每 15 分钟对所有 /srv/git/*.git 执行 chgrp -R fleet + chmod -R g+w；**共享锁文件一并治理**——fade-hook.lock 类文件归属随推送者漂移，须 chgrp+chmod 666 或预建共享位）；③ 遇 push 无故被拒先查裸仓对象目录属主分布再查网络/凭证。

### D-11 审批按命令前缀整串匹配——禁复合 cd 形态（2026-08-27，p0fix1 三轮 blocked 根因判定）

CC 工具白名单规则（如 `Bash(git status:*)`）对**整条命令串做前缀匹配**：`cd /repo && git status` 以 `cd` 开头→零命中→拒。首次怀疑方向（Task 子代理不继承 --allowedTools）经 sg 判定实验证伪——子代理完全继承白名单；真因即复合形态。行为规则：① 编排 spawn 必须把工作仓路径直接作为会话 cwd（orchestrate_tick 已按 tree.repo 字段路由），使执行体用裸命令即可；② 执行体跨仓用 `git -C <路径> …`；③ BRIEF_V2 已固化该铁律；④ 排查"工具被拒"类问题先取**原始拒绝文本**分层定位（审批层 vs 权限层 vs 上游），禁凭表象归因。

### D-12 Windows 原生操作优先 PowerShell 工具——Git Bash 的 MSYS 路径转换陷阱（2026-08-28，LG-002 终验探测误判复盘）

已设 `CLAUDE_CODE_USE_POWERSHELL_TOOL` 只表示**增加** PowerShell 工具，不禁用 Bash——工具选型仍是每次调用的判断。Git Bash 的 MSYS 层会把 `/v`、`/d` 类参数转换成路径（reg query 的 `/v` 被吃→token 读空→健康系统误判 401，LG-002 终验探测实录）。行为规则：① Windows 原生操作（registry/env/服务/Windows 路径/含 `/x` 单字母参数的命令）一律优先 PowerShell 工具；② Git Bash 仅用于 POSIX 管道与 ssh 场景；③ 判据口诀：命令里有反斜杠路径或单字母斜杠参数=PowerShell。

### D-13 通信名址规程与命名宪法——ListAgents 对名址+双向纪律+15 席正名表（2026-09-01，令源存疑事件与名址三易复盘）

根因：常驻会话名址三易（team-lead→trimetaverse-ec→f9）+自称董事会之来件无从对名址校验→2026-09-01 08:0x 令源存疑安全事件（治理面冻结，以编号解冻令 U-20260901-01 经 CEO 在席确认解冻）。通信面无固定名址=防伪校验无锚。

行为规则：

1. **发件前 ListAgents 对名址**：寻址一律用对方**正名**；正名不唯一时附 ListAgents 列表 ref 消歧；收到自称某席的来件，先 ListAgents 核对该名址在册再做治理性动作（无编号之恢复/解冻类来件一律视伪）。
2. **双向纪律**：呈报方核通道、转呈方核结论——转呈前必须核对原结论，禁转发未核（2026-09-01 调查树批复立，由单向教训「零日志先核重定向面」升格）。
3. **命名宪法全文表（CEO 2026-09-01 裁定）**：正名原则=职位代号；别名=中文名/职位全称/英文名，可空缺候补。

| 正名 | 职位 | 别名（中文名/职位全称/英文名） |
| --- | --- | --- |
| BOD | 董事会 | 董事会 |
| COS | 总裁助理 | 小贾/总裁助理/jarvis |
| CPO | 产品总裁 | 小乔/产品总裁/jobs |
| CTO | 技术总裁 | 小狄/技术总裁/Dee |
| CHO | 人力资源与交接治理负责人 | 小源（按册补录 2026-09-14，roster 实证） |
| CAO | 首席行政官 | 小行（CEO 正式命名 2026-08-01；E1 四追平 2026-09-14） |
| COO | 首席运营官 | 小营（按册补录 2026-09-14，roster 实证） |
| CFO | 首席财务官 | 小财（按册补录 2026-09-14，roster 实证） |
| CMO | 首席营销官 | 小敏（按册补录 2026-09-14，roster 实证） |
| CSO | 首席客户成功官 | 小成（按册补录 2026-09-14，roster 实证） |
| FD | 全栈开发 | 小全/全栈开发 |
| ST | 测试 | 小柯/测试 |
| RDT | 研发培训 | 小吴/培训 |
| DE | 部署 | 部署人员 |
| BL | 业务组长 | 候补录（候选「业务 lead」；中文名空缺候补） |

4. **spawn 面 frontmatter name 不改原则**（§8-2 先例 breaking）——正名属通信面/常驻会话面；FD/ST/RDT/DE 通信面正名↔spawn 型映射：FSD/STE（SeniorTestEngineer）/RAndDTrainer/SDE。

   **条 4 勘误与破例（2026-09-03，LG-029 锚，CEO 方案 v3）**：①ST 行映射「↔TestEngineer」勘误为「↔STE（SeniorTestEngineer）」，同批 FD 行 FullStackDeveloper→FSD；②「一律不改」原则对 FSD/STE 两席显式破例——spawn name 随批改，CAO 席 agent 类型面实测 FSD/STE 已在役、旧名 FullStackDeveloper/TestEngineer 退役（2026-09-03 22:3x）；③CHO 历史名沿革口径四条随勘误对表入册：历史名冻结／映射行承载／检索口径／role-id 分轨注记（议题卡正身归 LG-029 卷）。
5. 宣贯落地读数（2026-09-01 四席全达）：CPO/CTO 独立会话直送（msg 7b6743a2/a3fe4295）；FD d433c445/ST 24fff145 双送达。各员真源 description 批量补别名关键词=CHO 域同批（LG-024 fast-follow 同窗候排期）。

**BL 席增设注记（2026-09-02，BOD 裁决全包采纳，LG-026 P0 件）**：BL=Business Lead（业务组长），daemon 常驻信件督办岗，挂 COS 麾下（项目负责人管理）。格式冻结：`BL-<项目代号>`（per-project 授名候业务出现再授，现在只冻格式不预留名——按需授名）。首任惯例（CAO 裁 2026-09-02）：**单组长起步=正名 BL 无后缀**——席位正名本身即寻址名，`BL-<项目代号>` 仅候多项目分席按冻结格式授名（GEN 非项目代号，不采用，防格式外 token）。扩展评估触发线（满足其一）：日均 200 封×连续 7 天／并行活跃项目≥3／组长单点故障积压事故≥1 次。实际岗位启用（合同/五件套/binding）时走 CHO 侧 handoff 流程，本条只登记通信面席位。

**命名前缀律与双位名址增补（2026-09-09，CEO 亲定，COS 派工修正）**：

1. **前缀律**：M 面全体员工会话名（-n 实例名）加 **m-** 前缀、R 面加 **r-** 前缀——面归属机器可读标记，跨机路由按前缀分发（LG-033 双 daemon 路由桥对接语义）。**席位正名不变**（宪法表照旧——前缀系实例名标记非正名变更，「正名跨面唯一」原则不破）。
2. **双位名址注册（LG-033 双 COS 定名四步流程之②就此办结）**：`m-cos`=交互位中枢（本机）｜`m-duty-cos`=值班位（sg）；各席实例名=m-<正名小写>（m-cao/m-cto/m-fd/m-ste 等，现役会话面以映射表现势为准）；r- 系=R 面未来席位预留。
3. **征名候选沿革备查**：双位征名 8 席候选（cos-hub/hub-anchor/local-hub/keel-hub/jarvis-hub/hub-axle/orbit-hub 系，含 CAO 投案 local-hub×sg-duty 配对）因 CEO 系统性前缀律方案**未采为终名**——各席创意存档备查；裁据=**机器可读路由标记>趣味语义**（路由层机器可读优先的架构裁决）。

### D-14 跨仓相对路径审计须显式声明审计根（2026-09-01，LG-023 二次勘误案复盘）

根因：fade-protocol-spec.md/fade-registry.md 实盘在 `TriCompany/docs/engineering/`，历代审计（董事会实勘/晨检/r5 三人组五源重建）均从 TriMetaverse 根扫起→三重审计集体误判「无实盘」；被引用路径系 TriCompany 仓根相对路径而审计根错位，被审计方边编辑边宣告「不存在」的自证锚一并如实入卷。

行为规则：

1. 跨仓相对路径审计**先显式声明审计根**（从哪个仓/目录起扫），结论句必须带根声明；
2. 「无实盘/不存在」类结论**必须全仓扫过且扫对根**后才可下（「答不存在前必须全仓扫过」纪律的跨仓扩展）；
3. **实测同文≠因果**：找到同名/同文文件≠找到因果镜像正身，镜像件勘误须另核渊源（镜像件勘误同族教训同批入册）。

### D-15 CPO+CTO 双席联审门与开发测试分派枢纽（2026-09-01，CEO 新规程立法）

规程内容（CEO 2026-09-01 裁定，即刻生效向前适用）：

1. **必要功能和模块=CPO+CTO 双席联审门**——单席不得自决；
2. **开发与测试分派枢纽=CTO**——FD 承接开发、ST 承接测试，一律从 CTO 手里派，**不接受越手直派**。

宣贯落地读数（2026-09-01 四席全达）：CPO/CTO 独立会话直送（msg 7b6743a2/a3fe4295）；FD d433c445/ST 24fff145 双送达。中枢路由领受：功能/模块令先送联审再分派。

注：宣贯令原建议号位 D-12 已被 2026-08-28「MSYS 路径转换陷阱」条占用——号位勘误（D-12→D-15）随入册留痕。

**v2 增补（2026-09-02，LG-026 重审复盘教训制度化；BOD 六裁点⑥采）——联审门第四核查项「能力底座核查」**：业务规则定稿前，产品+技术**双签**核对该宿主面是否具备规则所需的**通信/持久化原语**。案源：LG-026 双席联审 premised on「组长可通信」未被识破——R 面 agent-core 系 CC 移植子集无跨会话消息能力，而收发督办枢纽五项核心职能（收/发/督办/状态流转/升级）全部以跨会话通信为底座，岗位设计 0% 达成（**结构性 0**：非实现质量打折，是能力底座缺失致岗位本身不成立）。行为规则：①联审清单固定第四项=能力底座核查，业务规则定稿前双签过门；②前提性能力假设（「XX 可通信/可持久化/可调度」类）必须显式核对宿主面实盘，禁默认成立（「会话」被降格为「函数调用」即本例盲区形态——agent-core 被当 CC 等价物评估）；③核查结论随联审记录留痕。号位裁定（CAO 裁）：**D-15 v2 就地增补，不立 D-16**——联审门定义保持单条目可查照，本文件 D-03/D-04 版次增补先例同构。案源正身：`TriMetaverse/docs/execution/lg026-re-review-report.md` §一.2/§二问 0/§三。

### D-16 约束面内容域路由与发布控制（2026-09-03，LG-028 立法，BOD 七点全裁；初稿候 CHO/CAO 验证）

**载体（裁①）**：双轨——规则本体=本条；内容×席/宿主映射=governance-memory-index GID 条目（三环收口：内容 owner 提交→索引 owner 收口写入→MEMORY.md 指针行；条目稿随立法件交付索引 owner）。三约束面定性=**派生渲染面**，禁人工直改。

**三面分工（裁②③）**：

1. **CLAUDE.md=客观结构面**：只许装客观结构描述+路由导航+分权制中性指针行，迁后三段式（客观结构/路由导航/分权制中性指针行）；域知识（具体模块产品事实/技术细节/测试口径/当前排期/命令集本体/文件惯例细则）一律随席路由，本面只留指针。
2. **session 面=域知识主承载**：迁出域知识落各席 session 面新立「D 类域知识族」；spawn/session 同一规则真源两渲染目标，无第三形态（裁⑤）。
3. **spawn 面=发现与骨架面**：name/description/tools 骨架供宿主发现，域知识不入。

**正面判据（防扯皮条款）**：许留结构面=模块布局与兄弟仓路径／职责域 owner 矩阵摘要（更新触发=岗位职责变动 handoff 链 CHO 门同步——CHO 验证建议注记采纳，摘要与 staffing 真源联动防漂）／registry 路由优先序／迁出件路由导航指针／分权制中性指针行；禁止=上条域知识清单内容正文。

**迁出四件+指针质量（裁③）**：Common Commands→CTO 面；File Conventions→CAO 纪律册（本文件附录，候实施落位）；Weekly Operating Records→COS 面；v0.9.x 双轨→BusinessStrategy 面。**指针质量=验收读数项**：每条迁出件在 CLAUDE.md 保留路由指针行（两要素=目标面正名+真源路径），按指针一步可达真源，失联=验收不过（防迁出即失联）。

**维护控制（裁①⑥）**：三面唯一合法更新通道=源侧改→管线发布；**CLAUDE.md 入渲染管线（裁「入」，依据=CEO 令控死原文）**——真源=`TriCompany/docs/project-sources/trimetaverse-claude-md.md`（GID-05），管线=FADE-002 双条目（在册）。三面管线并轨表：

| 约束面 | 真源 | 发布管线 |
| --- | --- | --- |
| CLAUDE.md | TriCompany/docs/project-sources/trimetaverse-claude-md.md（GID-05） | FADE-002 双条目（在册） |
| session 面（.claude/hub/*.session.md） | source-agents 合同 sessionBody | claude-session 渲染条目（LG-023 在册，LG-024 扩席在飞） |
| spawn 面（.claude/agents+.github/agents） | source-agents 五件套/contract | source_publish_check --publish-agents（D-07 通道） |

**处置**：违手改=hash 不一致时下次 publish 覆盖+审计留痕（D-07 处置三面通用化；byte-exact 判据 FADE-002 副本同步两役再证 2026-09-05——BOD 点单④④判例注，不另立条）。

**分权制自指改造界限（首执行件）**：只改叙述人称、不改分权制语义——语义真源=ceo-chief-of-staff-authorization-matrix（GID-08a）+岗位合同；施工走 CLAUDE.md 真源+FADE-002，禁直改发布面。

**非溯及（裁④）**：已产出已签批不返工；新内容候本条适用；勘定并行；机械件照常。

**platforms 强制声明（裁⑦，CPO 条款）**：本条映射面 GID 条目必须显式声明 platforms 全键，禁依赖缺省（与索引头「权限与审批/工具选型/daemon 与进程」强制声明条款同族，扩及路由类条目）。

**向前适用+存量临时件**：生效向前适用；存量手作 hub session 件（含 CAO 本席 session 文件）走 LG-024 管线化/退役路径销账，销账前维持 interim 标注勿作真源；LG-024 管线批对表并入实施排期（裁⑥连带）。

**状态（2026-09-04 转正）**：CHO 内容面验证**过**——正式签收件 c55232e（七裁点逐项对表全过+自指界限专项过+批 2 放行前置确认，CAO 自验回避程序合规记档）；CTO 管线面对表锚 2d71d5e 在案（已纳 6 项；域外缺口二〔GID 收口+platforms 校验〕候 CTO 认账，属实施纳门面不阻转正）。FADE 发布链控死+publish_check 纳门=分段闸下批 2 放行前置（不变）。**本条自此可生产级援引**。

### D-17 运行面关键连接变更须 CEO 明令（2026-09-04，LG-030 拓扑勘定）

TriRLC 与中央面的连接（实例形态=TRIMC_BASE_URL 注入）系**运行面关键连接**——任何变更（注入形态/目标地址/连接通道）须 **CEO 明令**后方可实施；拓扑勘定与诊断可先行，变更禁先斩后奏。锚=LG-030（2026-09-04 拓扑勘定，连接零改动裁定）。

号位裁定（CAO 裁）：**新 D-17，非 D-16 内嵌注记**——判据=本册 D-15 v2「主语连续性」先例：连接面变更的审批归属无既有条目持有该主语（D-16 主语=约束面文档内容，非同族；并入即跨域杂糅）。

### D-18 盘面断言三诚实——缺席/存在/检索形态（2026-09-05，BOD 点单④批量入册；台账 LG-024/031/032 修订史锚）

根因族：以检索/匹配结果直接下结论，今夜连发两型——**检索形态伪阴性**：`dir/**` 父路径模式静默无命中被误读「未落盘」（COO 第二方法自证破案；「manifest 身份验证先于缺席断言」教训族 glob 型）；**幻影真源**：「合同真源 D-04」悬空引用长期在册而 D-04 正身实无 M-001 段——BOD 判语「悬空引用若非管线真读正身将长期潜伏」，修复例=M-001 段入册（f9a8271，同窗）。

行为规则：

1. **缺席断言**（「无实盘/未落盘/零命中」）→ 全仓扫过+扫对根+形态复核（D-14 同族延伸）；
2. **存在/真源引用**（「真源=X」）→ 必真读 X 正身至少一次，禁凭二手转述维持引用（幻影真源防线）；**域路由指针先实勘后引用——失联即门退回报，不猜路径改写；候初始化真源如实申报不预支，初始化后回填**（CMO 基线升通用案，CHO 裁 2026-09-05；公共段通道单源注入=b149952 常量扩第五件，管线面 CTO 域另办；正名/ListAgents/date 三件留席内基线段不升公共段）；
3. **检索零命中** → 先疑 pattern 形态（父路径 glob/大小写/分隔符）再疑对象不存在，第二方法交叉验证后定言。

### D-19 证据判读先验时点与通道（2026-09-05，BOD 点单④②；台账 LG-031/032 锚）

根因：凌晨零条目被读作「通道异常」——实为前置未齐瞬态（CTO 自撤双轨说：session 面执行通道主链为正）；断点读数新旧未验致误判常态/异常。

行为规则：

1. 拿到任何读数/断点/日志先问「**这是什么时候的状态**」——时点新旧先验，陈旧读数不作现势结论（D-04 现查纪律的证据面延伸）；
2. 「零条目/零输出」先排除瞬态（前置未齐/窗口未到）再定性通道缺陷；
3. 通道定性以主链实证为准，双轨/旁路说须实证支撑，禁凭猜测定谳。

### D-20 修假设非修参数值（2026-09-05，BOD 点单④⑦；0fcd4cc 写根 b 案，BOD 裁语）

根因：修参数值平息一次、二次发作——复发即证明该修的是**假设/机制层**，不是参数值层。

行为规则：

1. 修复前先分层定性：参数值错（一次性环境值）or 假设错（机制/前提）——不同层不同修法；
2. **症状复发=修复层错误的证据**：同层重复修值禁超过一次，复发即升级重定性假设层；
3. 修复 commit 注明所修层级（值层/假设层），复发时可审计。

### D-21 草案落点规则（2026-09-07，BOD 三件执行令②立规）

规则正文（CEO 令照录）：**「工程任务类→engineering/；治理/执行提案类→execution/；单模块→本模块 docs/；跨模块→TriMetaverse docs/」**。

存量追认对表（合规，规则向前适用）：RDT 两件现落点均合规——event-watch 任务书留 `TriCompany/docs/engineering/`（ab20f94）✓；治理提案集留 `TriMetaverse/docs/execution/`（adeba0d9）✓。

关系注记：本规系提案 A（任务方案四级生命周期，adeba0d9 候审）的先行简化规；提案 A 批后并轨扩版（本条版次演进承载）；与既有「设计文档落点惯例」（execution=设计执行文档、engineering=技术真源）同向，自此升格 BOD 立法。

### D-22 best-effort catch 吞错禁令——静默降级温床（2026-09-08，BOD 裁「重要级」转；FSD 8ec8fe9 勘修件）

根因：connection-state.json persistState 内嵌 require 触发 ESM ReferenceError，被 best-effort catch 静默吞——机制自落地**从未工作**（8ec8fe9 勘修）；「启用而永败」深于「没做」（假成功三连/LG-030 静默降级同族深水层）。

行为规则：

1. **catch 块必须显式化错误或显式放弃**——记日志/上报/状态标记三选一起步，禁空 catch 与静默吞；
2. 「best-effort」语义只许覆盖**非关键路径**且须留「尝试过+失败原因」痕迹；关键路径禁 best-effort 形态；
3. 机制落地验收须含**负路径断言**（破坏依赖证其报错，不仅证成功路径）——防「启用而永败」；
4. 排查「功能似在而不工作」类问题，**首查 catch 吞错面**（静默降级温床固定搜索位）。

锚=8ec8fe9（FSD 勘修）+14495d21（在卷）。

### D-23 高峰排窗纪律——可调度大 token 工作避开双计价高峰（2026-09-09，CEO 亲定，BOD 令 00:0x+08 周三）

规则本体（CEO 定谳照录）：

1. **GLM**：北京时间周一至周五 14:00-18:00 高峰期 token 官方计价 **3 倍**——大 token 消耗工作尽量避开；
2. **DeepSeek**：周一至周五 9:00-12:00、14:00-18:00 高峰计价=**2 倍精确档位**（与 GLM 3 倍档区分，混排按各自倍率分账），空闲价=高峰**一半**——同避；
3. 其余时段（含周末全天）=空闲档。

排窗指导表：

| 时段（北京） | GLM | DeepSeek | 排程指引 |
| --- | --- | --- | --- |
| 工作日 9:00-12:00 | 平价 | 高峰 2x | GLM 类可做，DS 类避开 |
| 工作日 14:00-18:00 | **3× 高峰** | 高峰 2x | **双高峰：大 token 批量禁排（急件例外）** |
| 工作日 18:00-24:00 | 空闲 | 空闲 | **最优批量窗** |
| 夜间 0:00-9:00 | 空闲 | 空闲 | 最优（值守照常） |
| 周末全天 | 空闲 | 空闲 | 最优 |

适用边界：约束对象=**可调度**的大 token 消耗工作（全量测试/批量 spawn/大规模文档生成/模型密集批处理/长会话密集操作/**会话重建与上下文重载密集操作**——爆溃恢复/re-baseline 批量刷/快照回放/事件风暴唤醒链）；可操作判据=单任务预期消耗显著高于日常交互量级（定量线候 CFO 首月读数后补，先例=首月只记不设限）；急件与交互必需不受限；交互位/值班位日常响应照常。

执行注（v1.1 CFO 会签并入 2026-09-09，签认锚=CFO 回执；**v1.2 DS 高峰 2x 精确档系 CEO 补充随批前补正**——CFO 单价表四档中 DS 高峰档配置值取 2x）：排程消费方=COS 排程面+各席批量作业自检；**高峰段用量占比读数=本条合规观测面**（生效后该段占比不降=消费方未执行——警示非处罚，CFO 财务面）；watcher 去抖/事件批量合并参数在高峰段取更激进档（成本参数联动，LG-033 M/I 件同族逻辑）；财务阈值联动细则（金额口径基线/枚段额度折算形态/单价表四档）随密钥正身 §2 修订窗 CEO 批另落，不阻本条生效。

批准记录：**COS 批准 2026-09-09**（审批权限定则〔三类保留外归 COS 批〕批件第二例；锚链 cd2f477→1444162→34432ba 推双远端平）——本条成纪律生效。

### D-24 跨机操作首行 hostname 断言（2026-09-09，CTO 自提+COS 附议；三犯自领案）

根因：CTO 同一会话三度跨机取数错位——最近一例=值班 PID 验证将本机 netstat 8711 端口 PID 误作 sg 值班 PID 报出（COS 独立复勘 1989835 勘正，CTO 三犯自领）。跨机会话上下文混存两机视角，机位身份无锚=取数归因全盘可错。

行为规则：

1. **跨机操作/取数首行必 `echo hostname`**（或等效命令），断言读数与预期机一致方可继续——不一致即停，禁「应该没错」跳过；
2. 跨机取数的结论句须带**机位锚**（读数出自哪台机的哪个面），与 D-14 审计根声明同构；
3. 会话中切换机位视角（本地↔sg）时，**每次切换后首操作重跑断言**——会话级缓存机位身份无效（三犯形态即同会话机位漂移）；
4. 同族引用：D-04 时刻现查（时间维度现查）的空间镜像——时刻现查防时区漂移，hostname 现查防机位漂移；结论面诚实规则见 D-18（引用不复制）。

锚=1989835（COS 复勘勘正）+CTO 三犯自领。

批准记录：**COS 批准 2026-09-09**（审批权限定则三类外常规纪律条，批锚=批准回执；84ea1ca 推 sg 平 local=sg 复验）——本条成纪律生效，CTO 三犯案闭环终锚齐（勘正领受+纪律入册+推平三件）。

### D-25 呈报大白话摘要与令文全文扩写（2026-09-11，CEO 立规即刻生效）

根因：呈报件带内部术语缩写直上 BOD/CEO 系转译损耗——BOD 转呈 CEO 不可直接用；令文面背景/判据缺失诱发下行猜测补全（猜测=误跑温床，同族教训：执行令时点交叉核对、命令链断言须断整链）。

行为规则（CEO 立规口径照录）：

1. **令文面**：CEO 下发任务经 BOD 理解扩写后下发——收到的令=全文完（背景+判据+验收标准）版，照单执行即可，不必猜缩写；
2. **回报面**：上投 BOD 的呈报件须附一段**「大白话摘要」**（不用内部术语缩写，讲清干了什么/结果如何/候什么决定），BOD 转呈 CEO 直接用；内部席间互报不受此限。

批准记录：CEO 立规（2026-09-11 03:41+0800 即刻生效），COS 宣贯；CAO 领受入册（号位裁定 D-25，宣贯件即令源——CEO 立规即批准本体，无另批程序；通信/回报纪律族归 D 册，D-13/D-04 同族先例）。

### D-26 引用简介纪律（2026-09-11，CEO 终审意见升格立法；LG-034-INST 批次）

根因：人读文档引用规则条目/制度文件/真源处若只写名称或条目号，读者（尤其新人 agent 与跨域席位）须二次跳转方可得规则要义——引用无简介即路由断点，跨文档理解成本随引用密度线性放大（LG-034 联审制度 V0.1 终审意见 2026-09-11 09:58 定性）。

行为规则：

1. 人读文档凡引用规则条目、制度文件、真源文档处，**必附「引用简介」**——形态=小字注或折叠段任一，内容=规则一句话要义+出处路径两要素；
2. **增量生效**：本条生效后新写/修订文档即适用；存量文档列清扫专项候选清单分批补齐，不强制立即返工；
3. 制度文件文末来源清单逐条标注**审批人+审批时点+元信息头三要素**；无审批记录条目降「参考」或入补批清单——**补批走呈报，不自裁**。

批准记录：CEO 晨报联审制度 V0.1 终审意见（2026-09-11 09:58+0800）升格立法；CAO 主笔拟条（LG-034-INST 批次，首例适用件=joint-review-orchestration-workflow.md V0.2），条文随对照表报 CEO 确认。

### D-27 树协议执行层标准与循环闭环（2026-09-14，CEO 立规 00:05/00:12 连发；LG-034 后执行层标准复位）

根因：本周执行波（LG-034/035）可自含打包的执行任务走了对话式派工而未走树协议——**BOD 派工漂移**（CEO 2026-09-14 00:05 指正）；对话链任务无树路径/收口报告锚，销账缺机械凭证，与 D-01 v2「完成判定以机械回执为记账依据」同族缺口。

行为规则：

1. **树协议=执行层标准**：凡可自含打包的执行任务一律走树协议（任务书→挂平面→face 路由拾取→树执行→收口报告）；
2. **销账锚**：台账销账必附树路径+收口报告指针——机械凭证，缺锚不销账；
3. **对话式派工限界**：仅用于裁决交互链；事后按任务书格式补档（补档非可选）；
4. **循环闭环**：裁决→台账积累→COS 筛合并（≥5 件/周二五试运行）→任务书攒批→挂平面→face 路由拾取→树执行→销账附指针→C 面沉淀。

批准记录：CEO 立规（2026-09-14 00:05/00:12 连发，教训条随条）；CAO 主笔拟条（号位裁定 D-27，D-26 后续位）；**CTO 会签候 2026-09-14 晨流水（候签注记，签后本条生效转正）**；B3/B4 批次=第一批回归树协议执行之活。

**v2 增补（2026-09-20，BOD 死锁事故定谳随录；归口共裁 CAO 裁+COS 同意）——直派同步台账义务（催办全覆盖）**：

1. **发生即同步**：BOD 直派任务（含联审/命题/一切非树形态派工）**发生即同步 COS 台账**（挂账+状态字段）。与条 3 的界分：补档=形态补救（对话式派工补任务书格式），本条=时点义务（发生瞬间入册，不等补档）——两义并存不互代。
2. **催办全覆盖+时效锚**：台账在册即催办池成员；催办时效锚=按任务性质定阈值（硬阈值型如「明晨 09:00」、常规型如 24h 软阈值），防催办节奏无据（COS 共裁补条）。
3. **案例引注**：2026-09-20 死锁事故（直派绕开治理闭环致双头失察）——派工形态逃逸治理闭环第二实证（第一=D-27 根因本身）；完整案例正文随事故卷。
4. **归口记录**：落点经归口共裁——§12 否（跨域杂糅：部署面 git 拓扑≠派工台账义务）/FADE-008 补章否（管线实例规范 vs 席位行为义务主语不同族，可加一行指针非必需）/D-27 v2 胜出（主语连续性+三端发现性+案例归位三理由；COS 同意 2026-09-20 22:4x，BOD 定谳回执随 COS 件）。

**v4 增补（2026-09-23，CEO 树协议规范 V2 五条定调 22:20；BOD 起草+COS 初拟+CAO 审校成文，候 CEO 无异议即成文）——树协议 V2（派工枢纽重划与执行标准修订）**：

1. **树根统一**：树协议发起以 COS 为根、收口归 COS——无第二根；
2. **派工枢纽重划**：COS 交单 COO → COO 向执行席派工 → COS 监督 COO；**BOD/COS 不直接派工**。例外三通道保留（跨面发起〔拓扑必需：sg 值席派工不经本机 COS 物理不可达〕/裁决交互链〔BOD/CEO 裁决直接指令系治理本体〕/应急特批〔紧缩令紧急止血同构逻辑〕）——各通道**发生即补档 COS 台账（挂账+状态）非可选**（与 v2 直派同步条同源扩展：v2 覆盖 BOD 直派，本款扩展至全部绕链通道；当事席 COS 立场随录）；
3. **双向催办（双层：COS 唯一催办枢纽〔策略与收口〕+m-duty-cos 节律执行面）**：COS 催 COO（执行面进度）＋COS 催 BOD（呈批/验收面待办）——两向都归 COS；节律执行面（定时催办+晨检发布）**下沉 m-duty-cos**（sg 值席 7×24）；**BOD 不催办**（职责终版收敛=只有验收和呈批；CEO 22:24 修正+22:30 终版+23:33 双层演进，原「BOD 催 COO」条款作废）（FADE-008 §8.10 对表随批）；
4. **串行优先**：一树完成再开下一树，并行须每树不欠账（CEO 明示暂缓除外）；
5. **总进度汇报标准格式**：七列表（任务书/树 id/简述/树任务链/当前进度节点/派发时间/是否完成——首例=2026-09-23 全量盘点表 84db8ac4；FADE-008 §8 对表随批）。

批准记录：CEO 2026-09-23 22:20 五条定调（「候 CEO 无异议即成文」）；BOD 起草+COS 初拟（例外三通道保留+补档硬义务，当事席立场随录）+CAO 审校成文（归口）；closeout workflow V0.2 对表点核过（COO 督办读数仍归 COO 本职非新责/COS 收口报 BOD 线不变，无冲突）。

**v5 增补（2026-09-24，CEO 00:12 批定名令；COO 枢纽签发流转）——任务进度总表（Task Inventory）三条款**（条 5 汇报标准正身细化）：

1. **定名条款**：正式名=**任务进度总表（Task Inventory）**；文件名随日版次 `task-inventory-YYYYMMDD.md`（首铸=task-inventory-20260923.md 84db8ac4；f6a045de=定名后首份日刷新版——勘正 2026-09-24，原转述误锚）；**挂位=条 5 总进度汇报标准格式正身即此表**（v4 条 5 七列表=表列规范，本条定名+载体）；
2. **节律条款**：随日收口批刷新+推 sg bare（与 wt/* 推送同批）；
3. **指令语条款**：「拉总表」=CEO/各席指令语，指此表（指令语入册=口径统一防同物异名）。

**v3 增补（2026-09-20，BOD 令 LG-037 隔夜催违规纠偏随录；素材=COS 两条+COO 四条全收，COO 四条系 LG-034 实操既有口径明文化零新设）——催办时点与质量制度（明文化）**：

**时机层（COS 两条）**：

1. **即时催办**：联审/双席协作**在办任务**素材齐备即催办（小时内）——**不预约隔夜时点**，「隔夜等明天再催」本身即违规（在办任务的时点预约=催办义务的擅自展期）。引注=2026-09-20 LG-037 纠偏案（隔夜催违规，BOD 纠偏+真源查找定谳：全 docs 零时点细则，「即时/周期催办」从未成文——本款即补此白）。
2. **周期催办**：挂账任务按任务性质定周期——硬阈值型（时点到即催）／常规型（24h 软阈值日催）。与 v2 条 2 时效锚咬合：v2 定「锚怎么设」，本款定「锚到怎么催」。

**质量与终点层（COO 四条）**：

3. **催办三要素**：超时事实+兜底绝对时限+超限后果——只喊不加限=问候不是催办。
4. **催办有终点**：首催→重申一次→到顶按已收意见先行汇总+缺席标注，不无限催。
5. **超时≠批准**：催办到顶不构成默认同意（保留条款）。
6. **催办留痕**：催办记录=绩效效率源账面（CHO 绩效机制数据源衔接）；周期默认挂节奏窗边界，不发明新时钟；机械留痕形态照 D-01 v2（口头催办不作记录依据）。

### D-28 文档事实基线时点标注（2026-09-18，CEO 准；RDT 席方法升通用）

根因：文档中事实性内容（状态/端口/名单/基线读数/教程命令等）随时间失效，读者无法判断「这条事实何时核实」——**无基线时点的文档事实=不可审计事实**。方法源=RDT 席域消化实践（凡引 daemon/仓名的教程头部带「事实基线时点」注记，名址变更时按基线批量筛查，实勘验证 5 分钟定位全部旧口径；fade-001 现行版头部注记=既有先例）；CEO 2026-09-18 20:0x 准推广全席。

行为规则：

1. **旁注基线时点**：凡文档/记录修改涉及事实性内容，应在旁标注核实基线时点（形态如「（2026-09-15 实核）」）；
2. **适用界**：事实性内容（状态/数字/名单/路径/端口/命令行为描述）适用；制度条文/路由指针/历史叙事段不强制（历史冻结件豁免）；
3. **两级时点并存**：元信息头 lastSyncedAt=文件级同步时点，本条=内容级核实锚——两级并存、条级更精；
4. **引用连带**：引用他文档事实时宜连同其基线时点一并引用（如「X（2026-09-15 实核）」）；基线时点过期≠事实为假，是「候复核」信号（D-18 同族）；
5. **与 D-26 衔接**：D-26 管引用处简介（要义+出处路径），本条管事实旁的核实基线时点——两纪律并用不互代。

批准记录：CEO 2026-09-18 20:0x 准（立法入册令）；方法源=RDT 席（域消化汇总件建议②，COS 流转）；CAO 主笔入册（号位裁定 D-28=D-27 后续位）。

### D-31 FADE 实例完整性三件套（2026-09-21，CEO 定性；河源翻周 ADE-only 死工作案）

根因：河源翻周实例=**ADE-only 死工作**——有触发、无 agent plan、无考卷：自动作业跑了但无计划可审、无验收判据可核，产出不可审计不可学习（FADE-008 Echo 段「C 面沉淀」无从谈起）。

行为规则：

1. **三件齐**：凡 FADE 实例（触发型自动作业）必须三件齐——①触发（cron/hook/事件）②agent plan（任务书/计划件）③考卷（验收判据/预期读数）——**缺任一=死工作，禁止放行**；
2. **补课义务**：发现存量死工作（ADE-only）→补 plan 与考卷（registry owner 补课，2026-09-21 河源案先例）——补课非可选；
3. **双三件套并置**：立规三件套（维护规则：条文+存量扫+整改排期）管**纪律条目完整性**，本条管 **FADE 实例完整性**——两套并置为公司两级完整性宪法（同族同窗收编，BOD 知会 2026-09-21）。

批准记录：CEO 2026-09-21 定性（FADE-001 补课+三件套收编令，BOD 知会转办）；CAO 主笔入册（号位裁定 D-31=D-30 后续位）；registry 条目归 COS（registry owner）并行已办——双落闭环。

### D-32 席位 resume 前转录水位检查（2026-09-22，CEO 令；FSD 挂起实锚）

根因：launch-seat 标准形态带 `--resume`——深水区旧转录（实证 FSD 97%）被原样加载回，每次重启背同一锅恶性上下文：挂起→resume→重挂起循环（今日 FSD 挂起实锚，BOD 11:4x 通报复活根因勘正）。

行为规则：

1. **resume 前查水位**：席位复活（launch-seat `--resume`）前先查转录水位（transcript 占用率）；
2. **深水区两择一**：先受控压缩（fade-cos-compact-001 配方：全量快照归一→压缩重建→diff 核验）或弃转录重生（快照在档前提）——**禁原样加载深水转录直接复活**；
3. **浅水直通**：水位健康（阈值建议 ≤50%，首月试运行校准）可直接 `--resume`；
4. **与受控压缩教义衔接**：受控压缩原用于状态污染响应（CLAUDE.md 教义），本条把「深水区」纳入同类触发——水位即污染前兆。

批准记录：CEO 令（BOD 11:4x 通报 FSD 复活根因勘正）；COS 流转；CAO 主笔入册（号位裁定 D-32=D-31 后续位；主语判据=席位会话转录管理，与 D-03 daemon 进程重启不同族不并入；阈值档位首周试运行后校准）。

### D-33 管道截胡退出码禁令——git 断言禁走管道尾（2026-09-23，CEO 23:44 批；PUSH-RC 假红案）

根因：`git push | tail` 类管道命令的 `$?` 取得**管道尾（tail）的退出码**而非 git 的——GitHub fatal 被遮蔽，首推误判成功（假绿；实锚=4689eb9a 首推假绿+双面 ls-remote 复核勘定）。与「断言失败须断整链」教训同族（LG-037 族），主语=管道退出码语义，与前条断言整链互补不并入。

行为规则：

1. **git 断言禁走管道尾**：`git push/pull/fetch/commit` 等需判成败的命令，退出码断言必须在管道外直取（`git push && echo OK` 形态或 bash `PIPESTATUS`），禁 `git … | tail/head` 后取 `$?`；
2. **展示与断言分离**：需看输出时先落 log 再断言（`git push > push.log 2>&1; rc=$?`），展示读 log 不碰退出码；
3. **远端复核锚**：高危推拉（首推/强推/双落点）以 ls-remote/远端状态复核为准——本地退出码绿不单独作完成依据（D-01 v2 机械回执同族：本地假绿≠远端落地）。

批准记录：CEO 23:44 批；COO 枢纽签发流转（PUSH-RC 假红教训候选清单）；CAO 主笔入册（号位裁定 D-33=D-32 后续位）。

### D-30 部署位禁直写脚本——先源后部（2026-09-21，CEO 20:31 令立规；三席联审 CAO 主笔拟条，制度追认随批）

根因：运行脚本散落部署位（.fade/fleet 家）**无源文件、无存档、无部署流程**——机器迁移/重装即全数丢失且不可重建；双端同功能脚本漂移无同步机制（app.ts 5+5 红同族）；硬编码（路径/席名/MAP 表）改一处须裸编辑部署位（今晚多起）。CEO 定调：**运行脚本先写源文件（有存档）再做部署（生效），只做到部署=迁移不可移植**。

行为规则：

1. **先源后部**：任何运行脚本（.ps1/.sh/.py/.vbs 及同族）新建或修改，**先写源文件入真源位，再做部署**（复制/引用生效到部署位）——只做到部署不落源=违规（迁移不可移植）；
2. **部署位禁直写**：部署位（`.fade/**`、`/srv/fleet/**` 等生效位）禁直接新写/裸编辑脚本；已部署脚本须改时走「改源→再部署」，部署位视为渲产物/同步产物；
3. **工具脚本与运行数据分离**：脚本（可执行体）=资产应真源化；运行数据（log/flag/state/jsonl 账本）=留运行位照 ignore——两类不得混放混记；
4. **归档纪律**：真源脚本必附头部注记（用途/部署目标位/最近核实基线时点——D-28 同族），无注记视同未归档；
5. **例外窗**：紧急止血可在部署位临时改（运行连续性优先），但须 **24h 内回写源位**+部署位留临时痕——例外不豁免归档。

批准记录：CEO 2026-09-21 20:31 令立规+「今晚起即按此执行，制度追认随批」；三席联审 CAO 主笔拟条（号位裁定 D-30=D-29 后续位），CPO/CTO 会签随三席合流；真源位落位细则与存量整改清单见 `TriCompany/docs/workflow/script-asset-provenance.md`（细则篇，随合流批后成册）。

**v2 增补（2026-09-22，BOD 裁示准；LG-044 sync 归一首例随录）——脚本资产一代一实现原则**：

- **判语**：同一脚本资产位（同职责同部署面的工具件）任一时点只允许**一个现役实现代**；新代上位前必须完成旧代独有防线/功绩的**可证移植**（移植笔 commit 号留痕），并同步下架旧代（git rm+史注归档——git 史即档）。
- **禁则**：①双实现代并存运行；②重写丢教训（新代未承接旧代事故修复即上线）。
- **例外通道**：并行灰营（新旧代同跑比对新代达标后旧代限时下架）须 owner 明令+期限戳+对比读数随卷。
- **首例**：sync 双代归一（LG-044，py 淘汰保 ps1，CTO 判 2026-09-22 14:02；移植链 e2ff079→29158d0/1f78619 可证）。
- 号位注记：本原则系 D-30 主语族（脚本资产 sourceOfTruth 归属/归档纪律/变更流程）深化条款，**v2 就地增补不另立号位**（COS 候选「D-29/D-31 后续位」经号位裁归 D-30 v2——主语连续性判据，D-15 v2/D-17 先例同构）。

### D-29 Windows 计划任务无窗直启禁令（2026-09-20，CEO 指出；CTO 线拟条 CAO 归口）

根因：Windows 计划任务以 `powershell.exe` 直启脚本必闪 conhost 可见窗口（黑屏一闪=干扰用户+暴露后台机制细节）；同类错误不得重犯。

行为规则：

1. **禁直启**：本地 Windows 计划任务（schtasks/任务计划程序）跑 PowerShell 脚本，禁止以 powershell.exe 为任务动作直接启动；
2. **两种合规格式**：① **VBS/wscript 包装**——`.vbs` 以 `CreateObject("Wscript.Shell").Run "...", 0, False` 调起（第二参=0 隐藏窗口），任务动作指向 .vbs；② **pythonw.exe** 直启 Python 脚本（无窗解释器）；
3. **已立惯例案例**：Sync-Alert（`TriMetaverse/.fade/hourly-sync-alert.vbs` 包装 `hourly-sync-alert.ps1`，2026-09-20 20:2x 切换验证无窗）；
4. **适用界**：本地 Windows 计划任务域；Linux cron/服务域 systemd 无此问题不适用（Linux 无 conhost 语义）；交互式前台脚本不适用（用户主动要窗口的除外）。

批准记录：CEO 2026-09-20 23:09 指出立条；CTO 线拟条+CAO 归口入册；号位 D-29=D-28 后续位。

**v2 增补（2026-09-21，Seat-Watchdog 指向归档副本事故）——action 脚本路径须指运行正位，归档件禁入运行链**：

根因：Seat-Watchdog 计划任务 VBS 实际指向 operating-records 归档副本 `ops-seat-watchdog.ps1`——正位 `.fade/seat-watchdog.ps1` 的修正从未生效（旧脚本循环拉起弹错窗，CEO 03:12 抓获）。包装无窗（D-29 v1 合格）不等于指向正确——**「跑起来了」与「跑的是正位脚本」是两个断言**。

行为规则：

1. **正位断言**：计划任务 action（含 VBS 内层引用）指向的脚本路径必须为运行正位（现役工作目录如 `.fade/`）；归档目录（operating-records/_archive 等）文件**禁作任务 action 目标**——与复活纪律同族（归档件禁入运行链）在机器链的实例；
2. **指向性实证**：任务装好/改指向后，须做一次指向性验证（改正位脚本一处可观测变化→确认任务跑出该变化；或装时 diff 归档/正位路径防复制粘贴失配）；
3. **归档/搬迁连带**：脚本归档或搬迁时，连带审查全量计划任务 action 引用（BOD 已扫零残留先例）；旧名任务改指正位后，旧位置文件归档并加「归档件禁运行」头注。

**v3 增补（2026-09-21，注入器架构单侧复验漏洞）——退役/搬迁类操作后双侧复验硬规则**：

根因：注入器提包（TriMLC/TriRLC 双复制体退役）执行中，执行体退役后仅在 TriRLC 侧跑 tsc 自纠三处漏切——**TriMLC 侧未复验**，带病交付（4 处 TS2307+伴生测试件第 5 处，若 daemon 重启即断 13 席喂食粮道），CTO 总监督验收 tsc 实测截获（止血 c69432a）。

行为规则：

1. **双侧复验硬规则**：退役/搬迁/删除类操作涉及**多仓同构复制体**时，「一侧自纠」不成立——每仓各自跑 tsc+全量测试，全部绿方可报完工（单侧绿≠他侧绿，复制体两侧漏切概率独立）；
2. **完工读数与盘面一致**：完工报「tsc 清/逐位同」声明必须=报文回合内实测值（与 D-04 自证格式同族——声明与执行证据绑定）；
3. **dist 悬态连带**：复制体删除后，受影响仓的 dist 旧编译产物仍引用已删路径——**下次重启前必须重建 dist**（D-03 v3 同族硬前置），重建前该仓禁重启。

**D-04 v5 增补（2026-09-05，BOD M-001 联审终裁①实施前置；CTO/FSD 勘定「悬空引用」裁 a 案，COS 转办）——M-001 状态条机械合同（五字段）正身入册**（置于维护规则前独立段，见下）：

## 状态条机械合同（M-001，五字段）

每份状态条头部：① 第一个动作=date 现查，读数原样粘贴（粘贴前不写任何其他内容）；② 无读数不报时（写「未现查」）；③ 联审时作为运行证据呈报；④ 水位自估（低/中/高/临界）；⑤ 末次活动时刻（transcript mtime 现查，不可得以签发时刻代之并标注）。

> 入册注记：系 D-04 状态条面机械合同延伸正身（D-04 报时纪律的机械执行细则，主语同族）。FSD 实勘「合同真源 D-04」系悬空引用（D-04 正身原无 M-001 段），本节即悬空修复——台账 M-001 条「合同真源 D-04 v2/v4」自此实锚。材料源=CEO 席 session-body 渲染终态件（TriMetaverse f669ec1a）与 CHO 席 session-body 源件双版，CAO 会签内容面独立 diff 抽验两版逐字一致零漂移，FSD 供料与双版同文。原手抄尾句「合同真源 D-04」**采 FSD 略去案删除**（正身内自指冗余；渲染物尾注由管线常量统一缀，终裁口径）。抽取正则锚=`^## 状态条机械合同（M-001[^）]*）\s*$`（FSD 段头定稿），段体边界至下一 `## ` 节头——故本段置于「## 维护规则」前独立段（D-17 之后），段体零夹带。终裁①：管线运行时按本节抽取注入 13 席 session 面。

## 维护规则

- 新纪律事故复盘后追加（格式：D-XX + 日期 + 根因一句 + 行为规则）
- **立规三件套（2026-09-21 起，D-29 教训制度化）**：新纪律条入册必须同步三件——①条文（本册条目）②存量全扫（违反本条新规的既有存量逐件清单）③整改排期（逐件或批量整改的 owner 与窗口）——**缺任一件不结案**（立规未扫存量=直启闪屏又跑两天教训，D-29 案）
- 源侧为本文件；三端员工运行时经知识通道引用——不各自复制（防多真源）

## 附录·项目侧文件约定（2026-09-04 迁入，LG-028 D-16 迁出四件之一）

> 来源：TriMetaverse `CLAUDE.md` §File Conventions（迁入时四条正身实读 L120-125，与迁出令转述一致零漂移）；真源自此为本附录。CLAUDE.md 侧删节+路由指针行候 CLAUDE.md 真源批落（D-16 指针质量验收项配套，本侧先行落位如实注记）。

1. **GitHub Actions workflows**：commit 消息中人工归属用 `@username` 标注。
2. **AI commits**：尾注 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
3. **Markdown 表格**：分隔符用带空格形态 `| --- | --- |`（markdownlint 规范）。
4. **Agent 文件**：`.claude/agents/*.md`（Claude Code 宿主，tools 名 PascalCase）；`.github/agents/*.agent.md`（Copilot 宿主，tools 名 lowercase）。

**v6 增补（2026-09-24，CEO 03:09 批；COO 枢纽签发）——批准后 2h 派工时限与派工前置对表（防重+防漏成对条款）**：

1. **批准后 2h 派工时限**：任何 BOD/CEO 批准事项，COS 须在批后 **2h** 内发起实施派工或显式缓办列名（缓办须列名入台账候重启）——逾期未派工未列名=触发催 CEO（催办升级链顶格）。首例=LG-048 界面意见 48h 无动作催 CEO 案。**无时段豁免（2026-09-24 22:20 终版）**：22:00-08:00 窗内批准的派工仍须 2h 时限内经 M-SG NOTIFY 通道发起派工（转值席/责任席照常接线），夜间窗仅豁免「工作时段」属性不受 14-18 高峰窗限——防漏不打烊。
2. **派工前置对表**：派工发起前，派工方须对表核查——①该事项是否已有在办实现（防重复建设）②该事项是否已有在册纪律覆盖（防制度重立）——两项对表通过方可发起派工。存量清点参考=`trees/stale-dispatch-audit-20260924.md`（42e61bc4）。
3. **防重+防漏成对**：48h 时限防漏（批后不派=搁置），前置对表防重（派了已有=重复）——两条款成对生效缺一不完整。

批准记录：CEO 2026-09-24 03:09 批；COO 枢纽签发；CAO 主笔入册（号位裁定=D-27 v6=D-27 后续位同条目递增——派工时限与前置对表系 D-27 派工形态标准延伸条款）。

**v7 增补（2026-09-24，CEO 22:09 批；COS 初拟+CAO 落正身）——派工到达确认机制（三闸之三「防断」）**：

1. **接令回执强制**：树协议每一环（cos→coo→执行席、跨环转投、代转、补投）派工后，须获执行席**接令回执**（确认接手）方算派工闭环——无回执=未到达（断链态）；**「已发」不构成「已达」**；
2. **断链处置**：无回执超 **30 分钟**=派工断链——派工方即刻补投或改道；仍不通上呈 BOD；
3. **断链检测兼职**：m-duty-cos 30min 催办循环兼任断链检测——发现「宣称派工但执行席无接令回执」即报 BOD；
4. **衔接注记**：本条系 D-15 分派枢纽纪律「接令须回执确认接手」从枢纽纪律**扩展为树协议全链强制**（每一环均适用，含代转/补投形态）；
5. **三闸成套**：本条（防断）＋「批准后 2h 派工时限」（防漏，含夜间豁免）＋「派工前置对表」（防重）＝树协议派工三闸，成套适用。

批准记录：CEO 2026-09-24 22:09 批（树协议 V2 增补）；COS 初拟（草案 682563b8）+CAO 落正身；令源=LG-041 实施段断链实证（宣称派工≠到达，DE 窗无痕迹）。

**v8 增补（2026-09-24，CEO 21:58 批；COO 枢纽签发）——立项即登记（登记完整性条款）**：

新单立项/任务书落树时**同步登记** master-table.json（COS 铸总表/立项流程内自动联动，非人工补录）。与「登记完整性」条款合并（本日两缺口：core-agent 派工未对表+三闸评估立项未登记催办源——均为立项未登记导致的催办漏斗缺口）。随 D-27 v4 批落笔（派工枢纽重划+执行标准修订族延伸）。

批准记录：CEO 2026-09-24 21:58 批；COO 枢纽签发；COS 交单；CAO 主笔入册（号位裁定=D-27 v8=D-27 v7 后续位同条目递增——立项登记系派工前置对表的上游必要条件：未登记则前置对表无从查起）。
